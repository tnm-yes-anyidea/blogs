image: python:3.11-slim

pages:
  stage: deploy
  before_script:
    - apt-get update && apt-get install -y pandoc git
  script:
    # Run the builder script
    - python SCRIPTS/build.py
    
    # Assembly phase: move output into public/
    - mkdir -p public
    - cp index.html public/
    - cp -r SCRIPTS public/
    - for dir in */; do
        if [ "$dir" != "public/" ] && [ "$dir" != "SCRIPTS/" ] && [ "$dir" != ".github/" ]; then
          cp -r "$dir" public/;
        fi;
      done
  artifacts:
    paths:
      - public
  only:
    - main
